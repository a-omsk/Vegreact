import React from 'react';

const NoLocations = () => (
        <h3 style={{ textAlign: 'center' }}>
            Извините, в этом городе пока не добавлено ни одного заведения
        </h3>
    );

export default NoLocations;
