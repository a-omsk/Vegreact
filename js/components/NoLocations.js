import React, { PropTypes } from 'react'

const NoLocations = (props) => {
    return (
        <h3 style={{textAlign: 'center'}}>Извините, в этом городе пока не добавлено ни одного заведения</h3>
    )
}

export default NoLocations;
